# Minimal Mosquitto config for the microgrid sim (localhost-only, single machine).
# This is the source-of-truth config; `make run` / setup copies it to the brew
# path so `brew services start mosquitto` works as the manual documents.
listener 1883 localhost
allow_anonymous true
